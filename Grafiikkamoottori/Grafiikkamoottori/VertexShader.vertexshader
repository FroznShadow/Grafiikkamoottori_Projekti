#version 120 

#define VERTEX_POSITION 0
#define VERTEX_COLOR 1

in vec3 vertexPosition_modelspace;
in vec3 vertexColor;
varying out vec3 fragmentColor;

void main( )
{
	gl_Position.xyz = vertexPosition_modelspace;
	gl_Position.w = 1.0;
	fragmentColor = vertexColor;
}